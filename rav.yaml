scripts:
  server:
    - cd playdeep && python manage.py runserver
  makemigrations:
    - cd playdeep && python manage.py makemigrations
  migrate:
    - cd playdeep && python manage.py migrate
  shell:
    - cd playdeep && python manage.py shell
  require:
    - pip install -r requirements.txt

  
  